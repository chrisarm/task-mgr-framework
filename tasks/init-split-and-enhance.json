{
  "branchName": "feat/init-split-and-enhance",
  "description": "Split `task-mgr init --from-json X` into project-level `task-mgr init` (no PRD; scaffolds .task-mgr/, runs migrations, writes default config) and PRD-level `task-mgr loop init X` / `task-mgr batch init <glob>...`. The current `init --from-json X` form survives indefinitely as a deprecated shim. Add a new `task-mgr enhance` command that writes/updates a marker-fenced workflow block in CLAUDE.md and AGENTS.md so agents (Claude Code, Cursor, Aider) know how to use task-mgr correctly. `task-mgr init --enhance` is a convenience flag combining both. Loop and Batch top-level commands become parent-with-subcommand (Init/Run) with backwards-compat shim for the flat form.",
  "globalAcceptanceCriteria": {
    "criteria": [
      "Rust: No warnings in `cargo check` output (scoped to changed files per iteration; full at REVIEW-001)",
      "Rust: No warnings in `cargo clippy -- -D warnings` output",
      "Rust: All scoped tests pass with `cargo test -p task-mgr <module>` per iteration; full `cargo test` at REVIEW-001",
      "Rust: `cargo fmt --check` passes",
      "Rust: `cargo run --bin gen-docs -- --check` passes after FEAT-006 (CI gate)",
      "No breaking changes to existing `commands::init::init()` signature — its callers double after this PR (top-level Init shim + LoopCommand::Init + BatchCommand::Init), so any signature change is a refactor that must keep all three call sites compiling.",
      "Pattern: handle Result/Option explicitly — no unwrap() in production paths. Use `map_err(TaskMgrError::...)` for DB calls.",
      "Pattern: file writes go through atomic helpers — `marker_splice::write_atomic` for enhance, `write_default_model`'s tempfile pattern for config."
    ],
    "description": "These criteria apply to ALL implementation tasks"
  },
  "model": "claude-sonnet-4-6",
  "priorityPhilosophy": {
    "description": "Hierarchy of what matters most when implementing tasks",
    "hierarchy": [
      "1. PLAN — Anticipate edge cases before coding",
      "2. PHASE 2 FOUNDATION — ~1 day now to save ~2+ weeks later (1:10+ ratio); we are pre-launch, foundations compound",
      "3. FUNCTIONING CODE — Pragmatic, reliable, wired in per plan",
      "4. CORRECTNESS — Compiles, type-checks, scoped tests pass deterministically",
      "5. CODE QUALITY — Clean code, qualityDimensions satisfied, no warnings",
      "6. POLISH — Docs, formatting, minor improvements"
    ],
    "principles": [
      "Quality dimensions explicit — qualityDimensions on every task tells you what 'good' looks like",
      "Phase 2 foundation — prefer solutions that lay strong post-launch foundations (1:10+ savings ratio)",
      "Edge cases = test cases — every known edge case must have a corresponding test",
      "Scoped per-iteration tests, full suite at REVIEW-001 — REVIEW must leave the trunk green including pre-existing failures",
      "Indefinite shim — the deprecated `task-mgr init --from-json X` MUST keep working byte-for-byte after this PR; the canonical form is documented but the shim is permanent. Plan §11."
    ]
  },
  "prohibitedOutcomes": [
    "Removing or changing the shim's row-level behavior — `init --from-json X --append --update-existing` MUST produce the same DB state as `loop init X --append --update-existing`. This is enforced by a golden-file equivalence test in FEAT-005.",
    "Touching any PRD JSON file from the new project-level `init_project` function — that side effect belongs to `loop init` / `batch init` only.",
    "Auto-firing the interactive model picker from `loop init` or `batch init` — picker is project-level config and fires only from `task-mgr init` (project-level) and from the deprecated `init --from-json X` shim path.",
    "Adding a `--force` flag to project-level `task-mgr init` — project-level reset is `rm -rf .task-mgr/`. Reject `--force` with empty `from_json` and exit 2 with a clear stderr message.",
    "Hand-editing `.claude/commands/tasks.md` — it is regenerated by `cargo run --bin gen-docs`.",
    "Removing pattern matches over `Commands::Loop { .. }` / `Commands::Batch { .. }` from non-dispatch sites and assuming the old fields persist — Learning [1069]: every exhaustive match must be updated to the new variant shape.",
    "Using `std::fs::write` in the new `enhance` module — all writes go through `marker_splice::write_atomic` (FEAT-001) to avoid torn writes.",
    "Tests that only assert 'no crash' or check type without verifying content.",
    "Tests that mirror implementation internals (break when refactoring)."
  ],
  "project": "task-mgr",
  "requires": [],
  "taskPrefix": "b848fe20",
  "userStories": [
    {
      "acceptanceCriteria": [
        "Positive: `src/util/marker_splice.rs` exists with `pub fn splice_block(...)` and `pub fn write_atomic(...)`",
        "Positive: `src/bin/gen-docs.rs` imports both functions from `task_mgr::util::marker_splice` (grep confirms no local duplicates remain)",
        "Unit test: splice_block on empty input + replacement → result contains markers wrapping the replacement",
        "Unit test: splice_block on existing block → byte-for-byte replacement, content outside markers unchanged",
        "Unit test: splice_block called twice with same args → idempotent (second call's output == first call's output)",
        "Unit test: write_atomic writes via tempfile + rename in same dir; failure-to-persist case leaves target file unchanged (use a permission-denied or read-only-dir simulation)",
        "Existing gen-docs tests pass after the move: `cargo test --bin gen-docs` (or wherever the tests live post-move)",
        "CI gate: `cargo run --bin gen-docs -- --check` exits 0 (no diff in `.claude/commands/tasks.md` from the refactor)",
        "Negative: must NOT use `std::fs::write` in `marker_splice.rs` — all writes through tempfile pattern",
        "Negative: must NOT change `splice_block`'s public API after this task lands — FEAT-003 will consume it with the same signature",
        "CONTRACT: `splice_block` signature is `fn(current: &str, marker_begin: &str, marker_end: &str, replacement: &str) -> String`. FEAT-003 wires `<!-- TASK_MGR:BEGIN -->` / `<!-- TASK_MGR:END -->` through these params."
      ],
      "dependsOn": [],
      "description": "Move `splice_block` and the atomic-write helper from `src/bin/gen-docs.rs` (lines 177-214) into a new shared module `src/util/marker_splice.rs`. Update gen-docs.rs to import from the new location. This unblocks FEAT-003 (enhance) which will reuse both for managing the `<!-- TASK_MGR:BEGIN/END -->` block in CLAUDE.md / AGENTS.md.\n\nDO:\n- Create `src/util/marker_splice.rs` with `pub fn splice_block(current: &str, marker_begin: &str, marker_end: &str, replacement: &str) -> String` and `pub fn write_atomic(path: &Path, content: &str) -> io::Result<()>`.\n- Wire the module into `src/util/mod.rs` (create if it doesn't exist) and re-export from `src/lib.rs` if the binary needs `task_mgr::util::marker_splice::...`.\n- Update `src/bin/gen-docs.rs` to call the new module instead of local copies; preserve existing call sites and tests byte-for-byte.\n- Move (or re-add at the new location) the existing `test_splice_is_idempotent` test so coverage doesn't regress.\n\nDO NOT:\n- Change `splice_block`'s behavior. Marker params are arguments so the same fn serves `<!-- MODELS:BEGIN -->` (gen-docs) and `<!-- TASK_MGR:BEGIN -->` (enhance).\n- Hardcode marker strings inside `splice_block`.\n- Break `cargo run --bin gen-docs -- --check` — it must still pass after the refactor.\n\nEdge cases:\n- File has no marker block yet → match current gen-docs behavior (typically: append the block; verify what gen-docs actually does and preserve it).\n- File has marker_begin but no marker_end → return Err or current gen-docs behavior (verify).\n- Multiple marker pairs → replace first; preserve current gen-docs semantics.\n\nKnown-bad to guard against:\n- A naive impl that overwrites the whole file on splice — must preserve everything outside the markers.\n- Using `std::fs::write` directly instead of atomic tempfile+rename — torn writes on crash leave partial files.",
      "edgeCases": [
        "File has no marker block: append at end (matches current gen-docs behavior)",
        "File has unbalanced markers (begin without end, or vice versa): return Err with file:position context",
        "Multiple marker pairs in same file: replace first; current gen-docs behavior is the spec",
        "Write to read-only directory: write_atomic returns Err; target file unchanged"
      ],
      "estimatedEffort": "low",
      "failureModes": [
        {
          "cause": "Tempfile rename across filesystems (e.g. tmpfile in /tmp, target in /home)",
          "expectedBehavior": "Use same-directory tempfile (parent of target); rename is atomic within filesystem"
        },
        {
          "cause": "gen-docs CI check fails post-refactor due to subtle splice_block behavior drift",
          "expectedBehavior": "Bisect against pre-refactor splice_block; preserve exact semantics; do not 'improve' the function"
        }
      ],
      "id": "FEAT-001",
      "modifiesBehavior": false,
      "notes": "Source code to move: `src/bin/gen-docs.rs:177-214` (splice_block + write_atomic). Existing test pattern: gen-docs.rs:244 (`test_splice_is_idempotent`). Reuse pattern from `src/loop_engine/project_config.rs:215-221` (tempfile create + persist same-dir) — that's the production atomic-write idiom in this codebase.\n\nLearning [1093]: idempotent guard pattern — start with a symlink check before any file write. Apply to write_atomic.\n\nLearning [1075]: SetupFix struct pattern (name/description/applied/error) — not directly applicable here (no result struct needed for a low-level util), but informs FEAT-003's EnhanceResult.",
      "passes": false,
      "priority": 1,
      "qualityDimensions": [
        "splice_block byte-for-byte preserves content outside markers (regression test ensures)",
        "write_atomic never leaves partial files on crash (tempfile + rename pattern)",
        "Public API stable after this task — FEAT-003 will call into it without re-negotiation"
      ],
      "taskType": "implementation",
      "title": "Extract shared marker-splice util from gen-docs",
      "touchesFiles": [
        "src/util/marker_splice.rs",
        "src/util/mod.rs",
        "src/lib.rs",
        "src/bin/gen-docs.rs"
      ]
    },
    {
      "acceptanceCriteria": [
        "Positive: `init_project(fresh_tmpdir)` creates `.task-mgr/tasks.db` and `.task-mgr/config.json` with v1+ defaults",
        "Unit test: init_project on tmpdir with existing config.json containing `{\"customField\": \"keepme\"}` → after call, customField is still \"keepme\"; defaults filled in for missing keys (use the project_config.rs value-roundtrip pattern)",
        "Unit test: idempotency — call init_project twice; second call returns InitResult with `created_dirs: false`, `created_config: false`; file contents byte-identical between calls",
        "Unit test: tmpdir with pre-existing `tasks/foo.json` → init_project does NOT read foo.json; foo.json byte-identical before/after (use mtime + content hash assertion)",
        "Unit test: tmpdir with empty `.task-mgr/` (no DB, no config) → init_project creates both; subsequent calls are no-ops",
        "Unit test: non-TTY environment (override via IsTerminal injection or env var) → picker not invoked; init_project succeeds with no defaultModel set",
        "Negative: grep `src/commands/init/mod.rs::init_project` for `parse_prd`, `prefix`, `taskPrefix`, `tasks/` — none of these tokens appear in the function body or any helper it calls",
        "Negative: must NOT create `tasks/` subdirectory",
        "Negative: must NOT modify the existing `pub fn init(...)` (PRD-import) signature or behavior — gauntlet: `git diff` on the existing init() body shows zero changes after this task",
        "CONTRACT: InitResult variant or struct extension compiles against the existing `output_result(&result, cli.format)` call in main.rs — verify by running `cargo check` from main.rs's perspective",
        "Failure mode: if open_and_migrate fails (e.g., permission denied), init_project returns Err(TaskMgrError::...) with the underlying cause; no partial state in .task-mgr/"
      ],
      "dependsOn": [],
      "description": "Add `pub fn init_project(dir: &Path) -> TaskMgrResult<InitResult>` in `src/commands/init/mod.rs`. Project-level only: ensures `.task-mgr/` exists, runs migrations, writes a default `.task-mgr/config.json` preserving any existing keys, fires the model picker only if stdin+stderr are both TTYs. Idempotent. Does NOT read or write any PRD JSON.\n\nThe existing `pub fn init(...)` (the PRD-import function, currently at src/commands/init/mod.rs:182) stays exactly as-is — FEAT-005 will call it from the shim path and FEAT-004 will call it from LoopCommand::Init / BatchCommand::Init.\n\nDO:\n- Reuse `open_and_migrate(dir)` from `src/db/connection.rs:58` for DB scaffolding + migrations.\n- Reuse `ensure_directories(db_dir)` from `src/loop_engine/env.rs` for `.task-mgr/` subdir scaffolding.\n- Reuse the `serde_json::Value`-based read-mutate-write pattern from `write_default_model` (`src/loop_engine/project_config.rs:180-221`) so existing config keys (including unknown ones the user added) are preserved.\n- Fire the model picker via the existing helper in `src/commands/models/ensure_default.rs` only when both stdin and stderr are TTYs (non-TTY: skip silently per CLAUDE.md line 63).\n- Return an InitResult variant or extend the existing struct with fields like `created_dirs`, `created_config`, `migrations_applied` so the CLI can print a useful summary.\n\nDO NOT:\n- Touch any PRD JSON file. Do not call `parse_prd`, do not glob `tasks/`, do not invoke prefix write-back logic.\n- Accept a `--force` flag — project-level reset is `rm -rf .task-mgr/` per Plan §6.\n- Print the picker prompt when not a TTY — silent skip.\n- Hard-fail if `.task-mgr/` already exists — that's the idempotent re-init path.\n\nEdge cases (each must have a test):\n- Fresh tmpdir (no .task-mgr/) → create everything; config has v1+ defaults.\n- Existing config.json with user-set fields (e.g. `{\"customField\": \"x\", \"ollamaUrl\": \"http://custom\"}`) → preserve ALL existing fields; only fill in absent defaults.\n- Existing DB at older schema → migrations run cleanly; no data loss.\n- Non-TTY environment (e.g. test harness) → picker skipped silently.\n- `tasks/foo.json` exists in CWD → byte-identical before/after init_project.\n\nKnown-bad to guard against:\n- A naive impl that overwrites config.json wholesale, dropping unknown user-set keys (would pass a test that only checks `defaultModel` but is wrong).\n- An impl that creates `tasks/` directory — that's owned by loop init / batch init.",
      "edgeCases": [
        "Fresh tmpdir: create .task-mgr/ + DB + config",
        "Existing config with unknown user fields: preserve those fields verbatim",
        "Older-schema DB: migrations apply; no data loss",
        "Non-TTY: skip picker silently",
        "tasks/*.json present in CWD: do not read or touch"
      ],
      "estimatedEffort": "medium",
      "failureModes": [
        {
          "cause": "open_and_migrate fails (permission denied, disk full)",
          "expectedBehavior": "Return Err(TaskMgrError::...) with cause; .task-mgr/ may be partial but no PRD data corrupted (no PRD ever read)"
        },
        {
          "cause": "config.json write fails mid-rename",
          "expectedBehavior": "Tempfile pattern means target file is either old version or new version, never partial"
        }
      ],
      "id": "FEAT-002",
      "modifiesBehavior": false,
      "notes": "Reuse anchors:\n- `src/db/connection.rs::open_and_migrate` (line 58) — the implicit DB-creation path every command goes through today. Calling this explicitly is what makes init_project idempotent and gives the operator a clear entry point.\n- `src/loop_engine/env.rs::ensure_directories` (~line 85) — `.task-mgr/` scaffolding.\n- `src/loop_engine/project_config.rs::write_default_model` (line 180) — atomic write that preserves unknown fields via `serde_json::Value`. The pattern (read as Value, mutate as map, serialize, atomic tempfile rename) is what to copy.\n- `src/loop_engine/project_config.rs::read_project_config` (line 163) — defaults-on-missing-or-invalid behavior.\n- `src/commands/models/ensure_default.rs` — picker invocation when no model is resolved.\n- `src/commands/doctor/setup_fixes.rs::fix_generate_claude_md` (line 263) — the existing 'check exists then template-write' idempotent pattern; init_project's config-write should follow it.\n\nLearning [1093]: file-writing fix functions guard with symlink check first. Apply if init_project ever decides to overwrite .task-mgr/ contents (it shouldn't).\n\nLearning [1424]: 'task-mgr init requires --from-json' — this learning becomes INVALID after this PR. REVIEW-001 will invalidate it via `task-mgr learn --invalidate 1424`.",
      "passes": false,
      "priority": 2,
      "qualityDimensions": [
        "Idempotent (running twice = same result; no file mtimes change on second run unless content changes)",
        "Preserves existing config.json keys including unknown ones — uses Value-based roundtrip, not strongly-typed serialize",
        "Never touches PRD JSON (no `parse_prd`, no `tasks/` glob, no prefix logic)",
        "Picker fires only on TTY (matches CLAUDE.md line 63 contract)"
      ],
      "taskType": "implementation",
      "title": "Add project-level init_project function",
      "touchesFiles": [
        "src/commands/init/mod.rs",
        "src/commands/init/tests.rs"
      ]
    },
    {
      "acceptanceCriteria": [
        "Positive: `task-mgr enhance agents --create --target /tmp/fresh.md --profile workflow` exits 0; /tmp/fresh.md exists with `<!-- TASK_MGR:BEGIN -->` and `<!-- TASK_MGR:END -->` and the workflow content between them",
        "Unit test: run `enhance agents --target X` twice on a file containing user content before and after the marker block; second run produces byte-identical file (idempotency)",
        "Unit test: file with prelude + marker block + postlude → after `enhance agents` with new content, prelude/postlude unchanged byte-for-byte; marker contents replaced",
        "Unit test: neither CLAUDE.md nor AGENTS.md present in CWD, `--create` not set → exit 2; stderr lists both skipped",
        "Unit test: `enhance strip` on file with markers + user content → markers and block removed; user content preserved",
        "Unit test: `enhance show --profile workflow` prints to stdout; no file writes (verify via mtime on any candidate file)",
        "Unit test: `full` profile strictly contains `workflow` profile content (use length comparison + grep for a workflow-only token)",
        "Unit test: unbalanced markers (BEGIN without END) in target → returns Err; exit 2; stderr names the file",
        "Negative: grep `src/commands/enhance/` for `std::fs::write` returns zero hits — all writes through `marker_splice::write_atomic`",
        "Negative: must NOT modify any file path not listed in `--target` or the default-targets resolution result",
        "Negative: `enhance show` and `enhance agents --dry-run` must NOT produce any file mtime changes (test via before/after stat)",
        "CONTRACT: `EnhanceCommand` variant field names + types match what `src/main.rs` dispatch destructures — verify by `cargo check` after wiring",
        "CONTRACT: `splice_block(current, \"<!-- TASK_MGR:BEGIN -->\", \"<!-- TASK_MGR:END -->\", new_block)` is the only marker-manipulation path; grep enhance/ for any other splice or `marker_begin` literal",
        "Failure mode: target file path resolution fails (e.g., parent dir doesn't exist) → enhance reports per-target error in EnhanceResult; other targets still attempted"
      ],
      "dependsOn": [
        "FEAT-001"
      ],
      "description": "Add a new top-level command `task-mgr enhance` with three subcommands:\n- `agents [--target <path>...] [--dry-run] [--create] [--profile workflow|full]` — writes the task-mgr-managed block (marker-fenced `<!-- TASK_MGR:BEGIN -->` ... `<!-- TASK_MGR:END -->`) into CLAUDE.md and AGENTS.md (defaults; override with `--target`).\n- `show [--profile workflow|full]` — prints the rendered block to stdout. No file writes.\n- `strip [--target <path>...] [--dry-run]` — removes the marker block and its markers from target files.\n\nDefault targets when `--target` is empty: `./CLAUDE.md` AND `./AGENTS.md`. Each is touched only if it exists OR `--create` is set; otherwise skipped with a stderr note.\n\nProfiles (stored as `&'static str` consts in templates.rs):\n- `workflow` — task-mgr CLI cheat sheet (show, list, next, add --stdin, set-status), mid-loop sync pattern (`--append --update-existing`), `<task-status>` tag reference, spawn-fixup PRD-targeting gotcha, pointer to `task-mgr learn` / `recall` / `tm-decisions`. Mirrors §3a of the user-global CLAUDE.md, project-scoped.\n- `full` — `workflow` + general LLM-coding guidelines (Think Before Coding, Simplicity First, Surgical Changes, Goal-Driven Execution). The full guideline text is provided in the user's request and goes verbatim into the const.\n\nDO:\n- Create `src/commands/enhance/{mod.rs, templates.rs, tests.rs}` (3 files).\n- Define `EnhanceCommand` enum in `src/cli/commands.rs` with the three variants above.\n- Wire `Commands::Enhance { cmd: EnhanceCommand }` and dispatch arms in `src/main.rs`.\n- ALL file writes go through `task_mgr::util::marker_splice::write_atomic` (FEAT-001) — never `std::fs::write`.\n- ALL marker manipulation uses `task_mgr::util::marker_splice::splice_block` (FEAT-001).\n- Idempotent: running `enhance agents` twice on the same file produces byte-identical output.\n- Return an EnhanceResult struct (per-target: name, action_taken, error) implementing TextFormattable for `--format json` support, modeled on SetupFix (Learning [1075]).\n\nDO NOT:\n- Auto-invoke enhance from any other command in this task (FEAT-005 wires the `--enhance` flag on `init`).\n- Touch any file outside the resolved target list.\n- Use format-string interpolation in templates — they are static `&'static str` for v1. Project-specific interpolation is deferred per Plan §9.\n- Modify content outside the marker pair — splice_block guarantees this; do not introduce code paths that bypass it.\n- Print the rendered block to stdout from `agents` (use `show` for that, or `--dry-run`).\n\nEdge cases (each must have a test):\n- CLAUDE.md missing, `--create` not set → stderr 'skipping CLAUDE.md (not found; pass --create to create)'; exit 0 if any target succeeded, else 2.\n- CLAUDE.md missing, `--create` set → file created with marker block at start (after optional empty line).\n- CLAUDE.md exists without markers → block appended at end (newline separation).\n- CLAUDE.md exists WITH markers → block contents replaced; rest of file byte-identical.\n- Both CLAUDE.md AND AGENTS.md exist (default targets) → both updated.\n- `--target docs/custom.md` (single explicit) → only that file is touched; defaults NOT processed.\n- `--dry-run` → render to stdout via `splice_block` simulation; no writes; no exit-2 even if files missing.\n- Unbalanced markers in target (BEGIN without END) → return Err with file path + position; exit 2.\n- `strip` on file without markers → no-op; print 'no marker block found in <file>'; exit 0.\n\nKnown-bad to guard against:\n- A naive impl that uses `std::fs::write` for the final commit (torn writes on crash). Grep verifies write_atomic is the only write path.\n- An impl that replaces the entire file body instead of just the markers (would pass a test that only checks 'markers present' but is wrong).",
      "edgeCases": [
        "CLAUDE.md missing, no --create: skip + stderr; exit 2 if all targets skipped, else 0",
        "CLAUDE.md missing, --create set: file created with block",
        "File exists without markers: append block at end",
        "File exists with markers: replace contents between markers",
        "Both default targets present: both updated",
        "Single --target specified: only that file touched (defaults skipped)",
        "--dry-run: no writes; render to stdout",
        "Unbalanced markers in target: error with file path; exit 2",
        "strip on no-markers file: no-op + informational stderr"
      ],
      "estimatedEffort": "high",
      "failureModes": [
        {
          "cause": "write_atomic fails on one target (permission denied) while another succeeds",
          "expectedBehavior": "Per-target error captured in EnhanceResult; remaining targets still processed; exit code reflects partial failure (2 if any failed; 0 only if all succeeded)"
        },
        {
          "cause": "Unbalanced markers in existing CLAUDE.md (user manually edited)",
          "expectedBehavior": "Return Err with file path + line of unbalanced marker; do not attempt to 'fix' or guess"
        }
      ],
      "id": "FEAT-003",
      "model": "claude-opus-4-7",
      "modifiesBehavior": false,
      "notes": "Module layout mirrors `src/commands/models/` (mod.rs + handlers.rs + types.rs + tests.rs pattern). Reuse anchors:\n- `task_mgr::util::marker_splice::splice_block` and `::write_atomic` (FEAT-001) — the ONLY marker + write path.\n- `src/commands/doctor/setup_fixes.rs::fix_generate_claude_md` (line 263) — the existing CLAUDE.md template precedent; treat as design ancestor.\n- `src/commands/curate/output.rs` or `src/commands/models/handlers.rs` for the TextFormattable pattern (handlers.rs:54-60).\n- `src/commands/curate/types.rs` for the per-action result struct pattern (mirrors EnhanceResult shape).\n\nWorkflow profile content (from §3a of user-global CLAUDE.md, project-scoped):\n- CLI cheat sheet: `task-mgr show <id>`, `task-mgr list`, `task-mgr next`, `echo {...} | task-mgr add --stdin`, `task-mgr set-status <id> <status>`, `<task-status>X:done</task-status>` etc.\n- Mid-loop sync warning: NEVER run bare `task-mgr init --from-json X` on a running effort; always pair with `--append --update-existing`. Note: post this PR, `task-mgr loop init X --append --update-existing` is the canonical form (the shim still works indefinitely).\n- humanReviewOutcome pattern for CLARIFY tasks.\n- Spawn-fixup PRD-targeting: pass `--depended-on-by <milestone-id>` or `--from-json tasks/<correct-prd>.json` when spawning ad-hoc fixups (CODE-FIX, WIRE-FIX, IMPL-FIX, REFACTOR-N).\n- Pointer to `task-mgr learn` / `recall` / `tm-decisions` for learnings and decisions.\n\nFull profile = workflow + verbatim copy of the user's LLM-coding guidelines (Think Before Coding, Simplicity First, Surgical Changes, Goal-Driven Execution — the 4-section content from the user's request).\n\nLearning [1093]: idempotent guard pattern for file-writing fix functions — apply symlink check before write_atomic.\nLearning [1075]: SetupFix struct (name/description/applied/error) — EnhanceResult mirrors this shape so the existing TextFormattable + output_result formatters work.\nLearning [1405]: adding a subcommand variant requires changes across 6+ files (cli/commands.rs, commands/<name>/mod.rs, commands/<name>/types.rs, commands/<name>/output.rs, handlers.rs if present, main.rs). Check all of these are updated.",
      "passes": false,
      "priority": 3,
      "qualityDimensions": [
        "Marker-fenced: content outside markers is byte-identical before/after (splice_block guarantees)",
        "Idempotent: second run produces same file content (mtime may change due to atomic rename, but byte content matches)",
        "Atomic writes only: no `std::fs::write` anywhere in enhance/",
        "Templates are static `&'static str` consts — no runtime interpolation",
        "EnhanceResult per-target — one target failing doesn't abort the others"
      ],
      "taskType": "implementation",
      "title": "Add `task-mgr enhance` command (Agents/Show/Strip)",
      "touchesFiles": [
        "src/commands/enhance/mod.rs",
        "src/commands/enhance/templates.rs",
        "src/commands/enhance/tests.rs",
        "src/commands/mod.rs",
        "src/cli/commands.rs",
        "src/main.rs",
        "src/cli/tests.rs"
      ]
    },
    {
      "acceptanceCriteria": [
        "Unit test (parsing): `task-mgr loop init tasks/foo.json --append --update-existing` parses as `Commands::Loop { cmd: Some(LoopCommand::Init { prd_file, append: true, update_existing: true, .. }), prd_file: None, .. }`",
        "Unit test (parsing): `task-mgr loop run tasks/foo.json --yes --hours 0.5` parses as `Commands::Loop { cmd: Some(LoopCommand::Run { prd_file, yes: true, hours: Some(0.5), .. }), .. }`",
        "Unit test (parsing, flat-form shim): `task-mgr loop tasks/foo.json --yes` parses with `cmd: None, prd_file: Some(\"tasks/foo.json\"), yes: true`",
        "Unit test (dispatch, flat-form): on flat-form parse, dispatch synthesizes LoopCommand::Run with identical fields and emits stderr containing 'DEPRECATED'",
        "Unit test (parsing): `task-mgr batch init '*.json'` parses as `Commands::Batch { cmd: Some(BatchCommand::Init { patterns: [\"*.json\"], .. }), .. }`",
        "Unit test (parsing, flat-form): `task-mgr batch '*.json' --yes` parses with `cmd: None, patterns: [\"*.json\"], yes: true`",
        "All 27 existing Loop parsing tests in src/cli/tests.rs (lines 2191-2472) either survive verbatim (assertions over the flat form via cmd=None) OR are rewritten to LoopCommand::Run patterns — assertion content preserved either way",
        "All 10 existing Batch parsing tests (lines 2549-2666) similarly survive or are rewritten",
        "Negative: dispatch must NOT call into `commands::init::init()` from any Run arm — only Init arms call init()",
        "Negative: canonical `loop run` / `batch run` must NOT emit deprecation stderr (verify via captured stderr in test)",
        "Negative: `commands::init::init()` signature unchanged — `git diff src/commands/init/mod.rs` shows only addition of init_project (from FEAT-002), no changes to existing init()",
        "Failure mode: `task-mgr loop tasks/foo.json run` (both flat positional AND subcommand) → clap errors at parse time (`args_conflicts_with_subcommands`)",
        "CONTRACT: dispatch for LoopCommand::Init and BatchCommand::Init both call `commands::init::init(&dir, &paths, force, append, update_existing, dry_run, prefix_mode)` — same fn, same arg shape",
        "Learning [1069]: after the refactor, `cargo build` succeeds with zero exhaustive-match errors — grep `src/cli/tests.rs` for `Commands::Loop {` and `Commands::Batch {` post-change to confirm all matches reflect the new shape"
      ],
      "dependsOn": [],
      "description": "Reshape `Commands::Loop` and `Commands::Batch` from flat variants into parent-with-optional-subcommand using clap's `args_conflicts_with_subcommands` + `subcommand_negates_reqs` pattern. The parents keep all their existing fields as deprecated companions (positional becomes `Option<...>`) so `task-mgr loop tasks/foo.json --yes` still parses; dispatch synthesizes `LoopCommand::Run { ... }` and emits stderr deprecation.\n\nAdd subcommand enums:\n- `LoopCommand::Init { prd_file: PathBuf, force, append, update_existing, dry_run, prefix, no_prefix }` — calls existing `commands::init::init()` with `&[prd_file]`.\n- `LoopCommand::Run { prd_file: PathBuf, prompt_file, yes, hours, verbose, no_worktree, external_repo, cleanup_worktree, parallel }` — calls existing loop runner.\n- `BatchCommand::Init { patterns: Vec<String>, force, append, update_existing, dry_run, prefix, no_prefix }` — calls existing `init()` with globbed paths.\n- `BatchCommand::Run { patterns, max_iterations, yes, keep_worktrees, chain, parallel }` — calls existing batch runner.\n\nDispatch logic (per parent in src/main.rs):\n1. `cmd: Some(child)` → handle child directly.\n2. `cmd: None && (positional present)` (Loop: `prd_file.is_some()`; Batch: `!patterns.is_empty()`) → `eprintln!('DEPRECATED: prefer `task-mgr {loop|batch} run ...`. The flat form will continue to work indefinitely.')`; synthesize child from flat fields; dispatch.\n3. `cmd: None && no positional` → print help; exit 2.\n\nDO:\n- Apply `#[command(args_conflicts_with_subcommands = true, subcommand_negates_reqs = true)]` to the Loop/Batch parent structs (or the clap-4 equivalent attribute names).\n- Update EVERY pattern match over `Commands::Loop { .. }` / `Commands::Batch { .. }` in `src/cli/tests.rs` — there are ~37 of them. Learning [1069]: exhaustive matches break when fields change.\n- Add NEW parsing tests for the canonical forms: `task-mgr loop init <prd>`, `task-mgr loop run <prd>`, `task-mgr batch init <pat>`, `task-mgr batch run <pat>`.\n- Add NEW parsing tests for the flat-form shim: `task-mgr loop <prd>` parses with `cmd: None, prd_file: Some(...)`; dispatch wraps into Run.\n- Use `clap::CommandFactory` (Learning [160]) to programmatically introspect the new subcommand tree in tests, rather than spawning subprocesses.\n\nDO NOT:\n- Change `commands::init::init()` signature or behavior. It's called from THREE places after this PR (top-level shim, LoopCommand::Init, BatchCommand::Init); a signature change breaks all three.\n- Change behavior of the existing loop runner or batch runner. LoopCommand::Run / BatchCommand::Run just call the same underlying functions with the same args as today's flat dispatch.\n- Drop any flag from the flat-form fallback. `--yes`, `--hours`, `--prompt-file`, `--no-worktree`, `--external-repo`, `--cleanup-worktree`, `--parallel` (Loop) and `--max-iterations`, `--keep-worktrees`, `--chain`, `--parallel` (Batch) all still parse on the flat form.\n- Hide the deprecation message — `eprintln!` always (no `--quiet` opt-out in v1).\n- Pattern-match `Commands::Loop { prd_file, ... }` outside the dispatch arm. Only main.rs handles the flat shape; tests update to nested forms.\n\nEdge cases (each must have a test):\n- `task-mgr loop` (zero args, no subcommand) → print help; exit 2.\n- `task-mgr loop init` (subcommand but missing required prd_file) → clap usage error; exit 2.\n- `task-mgr loop tasks/foo.json` (deprecated positional) → stderr deprecation + dispatch to LoopCommand::Run with prd_file=Some('tasks/foo.json').\n- `task-mgr loop run tasks/foo.json` (canonical) → no deprecation; same behavior as today's flat form.\n- `task-mgr batch '*.json'` (deprecated positional) → stderr deprecation + dispatch to BatchCommand::Run with patterns=['*.json'].\n- `task-mgr batch init '*.json' '*.json'` (multi pattern on init) → parses fine; init() called with globbed paths.\n- File literally named `init` passed as positional to deprecated form (`task-mgr loop init` where `init` is a real file — collision with subcommand) → clap resolves as subcommand; user must use `task-mgr loop run init` or `task-mgr loop run ./init`. Document this in --help.\n\nKnown-bad to guard against:\n- A naive impl that uses two separate enums for nested vs flat (would double dispatch code paths; instead use Option<LoopCommand> with the flat fields on the parent).\n- An impl that emits the deprecation notice from the canonical `loop run` path (would spam stderr on every iteration).",
      "edgeCases": [
        "Zero args (`task-mgr loop`): print help, exit 2",
        "Subcommand missing required (`task-mgr loop init` without prd): clap error, exit 2",
        "Flat-form (`task-mgr loop tasks/foo.json`): deprecation + dispatch to Run",
        "Canonical (`task-mgr loop run tasks/foo.json`): no deprecation; same as old flat",
        "Multi-pattern batch init: globbed paths threaded to init()",
        "File literally named `init` collides with subcommand: documented escape `loop run ./init`"
      ],
      "estimatedEffort": "high",
      "failureModes": [
        {
          "cause": "Pattern-match update missed in src/cli/tests.rs (Learning [1069])",
          "expectedBehavior": "cargo build fails with non-exhaustive match error pointing at the unupdated test; fix in place"
        },
        {
          "cause": "args_conflicts_with_subcommands incompatible with required positional",
          "expectedBehavior": "Use subcommand_negates_reqs in addition; verify in a minimal prototype before refactoring the whole file"
        },
        {
          "cause": "Both flat-positional AND subcommand passed",
          "expectedBehavior": "clap rejects at parse time per args_conflicts_with_subcommands semantics; exit 2 with usage hint"
        }
      ],
      "id": "FEAT-004",
      "model": "claude-opus-4-7",
      "modifiesBehavior": true,
      "notes": "Reuse anchors:\n- `src/cli/commands.rs:66-97` (current Init), `:809-861` (current Loop), `:895-936` (current Batch) — the existing field set to preserve as deprecated companions.\n- `src/main.rs:116` (Init dispatch), `:803` (Loop), `:869` (Batch) — the existing dispatch arms; refactor each to handle nested + flat.\n- `commands::init::init` (`src/commands/init/mod.rs:182`) — call this from both LoopCommand::Init and BatchCommand::Init (with `&[prd_file]` and `&globbed[..]` respectively). Same signature, same behavior, two call sites instead of one.\n- Existing tests `src/cli/tests.rs:2191-2472` (Loop, ~27 patterns) and `:2549-2666` (Batch, ~10 patterns) — update each pattern.\n\nLearning [1069]: NEW fields on clap enum variants break exhaustive matches in tests. Every `Commands::Loop { .. }` and `Commands::Batch { .. }` match — including those in `src/cli/tests.rs` that test other parsing variants — must be updated. Use `cargo build` failure output as a checklist.\n\nLearning [1405]: adding a subcommand variant typically touches 6+ files. Same applies here for Loop and Batch nesting — verify cli/commands.rs + main.rs + cli/tests.rs + any handlers if extracted.\n\nLearning [160]: use `clap::CommandFactory` to introspect subcommands in tests rather than subprocess-based testing — faster + deterministic.",
      "passes": false,
      "priority": 4,
      "qualityDimensions": [
        "Flat-form shim works for every existing flag combination — no flag dropped",
        "Canonical form (loop run / batch run) suppresses deprecation noise — operators who migrate don't see warnings",
        "Existing init() function signature + behavior unchanged",
        "Pattern-match updates in src/cli/tests.rs preserve assertion content — only the wrapping pattern changes",
        "Tests use clap::CommandFactory for subcommand introspection (no subprocess spawns)"
      ],
      "taskType": "implementation",
      "title": "Nest Loop and Batch subcommands with flat-form shim",
      "touchesFiles": [
        "src/cli/commands.rs",
        "src/main.rs",
        "src/cli/tests.rs"
      ]
    },
    {
      "acceptanceCriteria": [
        "Unit test (parsing): `task-mgr init` parses with `from_json: vec![], enhance: false`",
        "Unit test (parsing): `task-mgr init --enhance` parses with `enhance: true`",
        "Unit test (parsing): `task-mgr init --from-json X.json` parses (deprecated shim form still accepted)",
        "Behavior test: `task-mgr init` in fresh tmpdir → init_project called; .task-mgr/ exists; stderr contains 'enhance agents' hint",
        "Behavior test: `task-mgr init --enhance` in fresh tmpdir → init_project AND handle_agents called; CLAUDE.md and AGENTS.md exist with TASK_MGR markers",
        "Behavior test: `task-mgr init --force` (no from_json) → exit 2; stderr explains 'rm -rf .task-mgr/'",
        "GOLDEN-FILE TEST (KEY): identical fixture PRD; run `init --from-json fixture.json --append --update-existing` on tmpdir A; run `loop init fixture.json --append --update-existing` on tmpdir B (both had init_project run first); assert `task-mgr list --format json --prefix <prefix>` output is byte-identical between A and B (after canonical JSON sort)",
        "Behavior test: `task-mgr init --from-json X.json` emits stderr DEPRECATED notice + runs picker (verify by setting up a fresh dir with no resolved model and asserting `.task-mgr/config.json` has defaultModel after the shim path, but NOT after `loop init X.json` directly)",
        "Behavior test: `task-mgr init --from-json X --enhance` → enhance ignored; stderr note explains; PRD still imported",
        "Negative: `commands::init::init()` signature unchanged — `git diff` confirms",
        "Negative: `loop init` and `batch init` direct invocations do NOT fire the picker (no defaultModel written by them)",
        "Negative: `task-mgr init --force` (no from_json) must NOT touch `.task-mgr/` — exit before any DB or config write",
        "CONTRACT: shim dispatch passes the same `&[PathBuf]` to `init()` that LoopCommand::Init / BatchCommand::Init dispatch does — single call site shape, two routing paths",
        "Failure mode: if init_project fails (e.g., permission denied), shim path also fails BEFORE any PRD import begins (verify ordering)"
      ],
      "dependsOn": [
        "FEAT-002",
        "FEAT-003",
        "FEAT-004"
      ],
      "description": "Reshape `Commands::Init` so `from_json: Vec<PathBuf>` becomes optional (was `required = true`). Behavior split in src/main.rs dispatch:\n- Empty `from_json` → call `init_project(dir)` (FEAT-002). Print stderr hint: 'Initialized .task-mgr/. Run `task-mgr enhance agents` to add task-mgr workflow guidance to CLAUDE.md / AGENTS.md.'\n- Non-empty `from_json` → stderr 'DEPRECATED: `task-mgr init --from-json X` keeps working indefinitely but the canonical form is `task-mgr loop init X` (single PRD) or `task-mgr batch init <glob>...` (multi).' THEN run `init_project(dir)` first (fires the picker per Plan §7) THEN dispatch the PRD payload via LoopCommand::Init (when N=1) or BatchCommand::Init (when N>1) — both call into existing `commands::init::init()` (PRD import code unchanged).\n\nAdd `#[arg(long, default_value_t = false)] enhance: bool` on `Commands::Init`:\n- `task-mgr init --enhance` → after init_project, also run `commands::enhance::handle_agents(dir, vec![], false, true, \"full\")` so both CLAUDE.md and AGENTS.md get created/updated with the full profile.\n- `--enhance` is silently IGNORED when `--from-json` is non-empty (shim path doesn't auto-enhance; print stderr note 'note: --enhance is ignored on the deprecated --from-json path; run `task-mgr init --enhance` separately').\n\nAdd `task-mgr init --force` (no `from_json`) → reject with stderr: 'Project-level init has no destructive form. To reset, `rm -rf .task-mgr/` and re-run.' Exit 2. Per Plan §6.\n\nDO:\n- Make `from_json: Vec<PathBuf>` optional (drop `required = true`). All companion flags (force, append, update_existing, dry_run, prefix, no_prefix) stay but are only honored when from_json is non-empty.\n- Add `--enhance` flag.\n- Implement the dispatch split in src/main.rs (project vs shim vs reject).\n- Add a GOLDEN-FILE equivalence test in `src/commands/init/tests.rs`: set up two identical fixture DBs (or two fixture dirs); run `init --from-json X --append --update-existing` on dir A and `loop init X --append --update-existing` on dir B (both already have init_project run); assert row+relationship+file equality.\n- Update `src/cli/tests.rs` Init parsing tests (lines 16-74) — split into 'new no-arg form' and 'deprecated shim form' groups.\n\nDO NOT:\n- Change row-level behavior of `commands::init::init()` — same fn, same args, two dispatch paths.\n- Drop the model picker side effect on the shim path. Operators who run `task-mgr init --from-json X` today get the picker; the shim preserves that by running `init_project(dir)` BEFORE the PRD dispatch.\n- Fire the picker from `loop init` or `batch init` direct invocations — those are PRD-scoped, not project-scoped.\n- Auto-run enhance when `--enhance` is NOT set. Bare `task-mgr init` only scaffolds + prints hint.\n- Forget to update `~/.claude/CLAUDE.md` text via FEAT-006 — that's the operator-facing doc.\n\nEdge cases (each must have a test):\n- `task-mgr init` (no args) → init_project + hint to stderr; exit 0.\n- `task-mgr init --enhance` → init_project + handle_agents(create=true, profile=full); both CLAUDE.md and AGENTS.md created/updated.\n- `task-mgr init --force` (no from_json) → exit 2; stderr explains alternative.\n- `task-mgr init --from-json X.json` (single PRD) → init_project (picker) + LoopCommand::Init { prd_file: X, .. } dispatch path.\n- `task-mgr init --from-json A.json --from-json B.json` (multi PRD) → init_project + BatchCommand::Init { patterns: [A.json, B.json], .. } dispatch.\n- `task-mgr init --from-json X --enhance` → enhance silently skipped; stderr note; PRD imported.\n\nKnown-bad to guard against:\n- An impl that calls `init()` directly from the shim instead of routing through LoopCommand::Init / BatchCommand::Init dispatch — would drift from canonical path semantics. The golden-file test catches this.\n- An impl that runs init_project AFTER PRD dispatch on the shim path — would skip the picker for operators who don't have a model resolved yet.",
      "edgeCases": [
        "No args: init_project + hint",
        "--enhance: init_project + enhance agents (create + full profile)",
        "--force (no from_json): reject with stderr; exit 2",
        "--from-json X.json (single): shim → init_project → LoopCommand::Init dispatch",
        "--from-json A.json --from-json B.json (multi): shim → init_project → BatchCommand::Init dispatch",
        "--from-json X --enhance: enhance ignored; stderr note; PRD imported"
      ],
      "estimatedEffort": "high",
      "failureModes": [
        {
          "cause": "init_project fails on shim path (permission denied, disk full)",
          "expectedBehavior": "Return Err BEFORE any PRD import; .task-mgr/ may be partial but no PRD data corrupted"
        },
        {
          "cause": "Golden-file test reveals shim vs canonical drift (different row state)",
          "expectedBehavior": "Bisect: most likely the shim path skipped a step or called init() with different args; fix the dispatch to mirror LoopCommand::Init exactly"
        },
        {
          "cause": "Picker fires on `loop init` direct invocation (regression)",
          "expectedBehavior": "Pin: picker is invoked from init_project only; loop init / batch init must NOT call init_project; they call init() (PRD import) directly"
        }
      ],
      "id": "FEAT-005",
      "model": "claude-opus-4-7",
      "modifiesBehavior": true,
      "notes": "Reuse anchors:\n- `init_project(dir)` from FEAT-002 — project-level scaffold + picker.\n- `commands::enhance::handle_agents(dir, target, dry_run, create, profile)` from FEAT-003 — called with `(dir, vec![], false, true, \"full\")` for the --enhance convenience.\n- `commands::init::init(dir, &json_files, force, append, update_existing, dry_run, prefix_mode)` (src/commands/init/mod.rs:182) — UNCHANGED; called from LoopCommand::Init (single) and BatchCommand::Init (multi) dispatch arms.\n- Picker helper in `src/commands/models/ensure_default.rs` — invoked from init_project per Plan §7.\n\nLearning [1424] becomes INVALID after this PR: 'task-mgr init requires --from-json' is no longer true. REVIEW-001 invalidates it via `task-mgr learn --invalidate 1424`.\n\nGolden-file test design: the cleanest implementation uses two tmpdirs with identical PRD JSON; runs both code paths; queries `task-mgr list --format json --prefix <auto-generated-prefix>` from each; sorts and compares. Alternative: direct SQL `SELECT * FROM tasks ORDER BY id` from both DBs and assert row equality.\n\nLearning [1505]: FK delete order matters in --force. We're NOT changing --force behavior (it stays on the loop init / batch init paths and on the deprecated shim) so this learning applies to verifying that path still works post-refactor.\n\nLearning [1685]: clap env var shadowing in tests. The new --enhance flag has no env binding so this doesn't apply, but be careful with future env-bound flags.",
      "passes": false,
      "priority": 5,
      "qualityDimensions": [
        "Golden-file equivalence: shim path produces identical DB state as canonical path — enforced by test, not just convention",
        "Picker timing on shim path preserves today's operator UX (picker fires before PRD import)",
        "Bare `task-mgr init` is fully non-interactive in non-TTY environments (CI-safe)",
        "--enhance is opt-in only (no surprise file mutations outside .task-mgr/)",
        "Project-level --force is rejected with a clear actionable error message"
      ],
      "taskType": "implementation",
      "title": "Top-level Init split: project-level + indefinite shim + --enhance flag",
      "touchesFiles": [
        "src/cli/commands.rs",
        "src/main.rs",
        "src/commands/init/mod.rs",
        "src/commands/init/tests.rs",
        "src/cli/tests.rs"
      ]
    },
    {
      "acceptanceCriteria": [
        "`cargo run --bin gen-docs -- --check` exits 0 (no stale doc) after the commit",
        "README.md contains `task-mgr loop init` (grep verifies)",
        "README.md contains `task-mgr batch init` (grep verifies)",
        "README.md contains a heading or subsection covering `task-mgr enhance` with examples for agents/show/strip",
        "README.md has a 'Project-level init vs PRD-level init' section explaining the split",
        "CLAUDE.md (project) line near line 63 references the new project-level `task-mgr init` and the picker behavior on both paths",
        "CLAUDE.md (project) has a 'Deprecation policy' subsection citing Plan §11 (shim permanence)",
        "Negative: must NOT remove existing `task-mgr init --from-json` example text — annotate as deprecated; keep the examples valid",
        "Negative: must NOT hand-edit `.claude/commands/tasks.md` — diff is from gen-docs only (grep the diff for any markdown not produced by gen-docs)",
        "Negative: must NOT modify `.task-mgr/`, `tasks/`, or `~/.claude/` from this task"
      ],
      "dependsOn": [
        "FEAT-005"
      ],
      "description": "Run `cargo run --bin gen-docs` to regenerate `.claude/commands/tasks.md` reflecting the new canonical commands (`task-mgr init` project-level, `task-mgr loop init` / `task-mgr batch init` for PRD import, `task-mgr enhance agents` for instruction file management). Update hand-edited operator docs. Add a 'Deprecation policy' note in CLAUDE.md (project) so future-self knows the shim lives indefinitely.\n\nFiles to edit:\n- `README.md` lines 64, 328, 465 (mentions of `task-mgr init --from-json`).\n- `CLAUDE.md` (project root) lines 63 (interactive picker description), 75 (PRD-JSON-in-two-places).\n- `.claude/commands/tasks.md` — regenerated by gen-docs.\n\nDO:\n- Run `cargo run --bin gen-docs` and commit the regenerated `.claude/commands/tasks.md` in the same commit as the doc edits.\n- Update README sections that show `task-mgr init --from-json` to show BOTH the new canonical form AND a brief note about the indefinite shim.\n- Add a new README subsection 'Project-level init vs PRD-level init' so first-time readers see the distinction.\n- Add a new README subsection 'Agent instructions: `task-mgr enhance`' covering the new command with one example each for `agents`, `show`, `strip`.\n- In CLAUDE.md (project), update line ~63 to describe the new picker behavior: fires from project-level `task-mgr init` AND from the deprecated `init --from-json X` shim path.\n- Update CLAUDE.md (project) line ~75's worktree note to use `task-mgr loop init` syntax instead of `task-mgr init --from-json`.\n- Add a new CLAUDE.md subsection 'Deprecation policy' stating the shim is permanent; cite Plan §11.\n\nDO NOT:\n- Hand-edit `.claude/commands/tasks.md` — it's generated by gen-docs. Run the binary, commit the diff.\n- Update `~/.claude/CLAUDE.md` (user-global) from this task — that file is outside the repo and the shim makes it lag-tolerant.\n- Update `mw-datalake/docs/TASK_MGR.md` from this task — separate repo; out of scope.\n- Delete existing `task-mgr init --from-json` examples — annotate them as deprecated; the shim is permanent so the examples remain valid.\n- Modify `.task-mgr/`, `tasks/`, or other state directories.\n\nEdge cases:\n- gen-docs regeneration produces a diff in `.claude/commands/tasks.md` → commit it; the CI gate `gen-docs --check` is what enforces freshness.\n- README's existing 'deprecated form' example is identical to new canonical form (rare) → no annotation needed.\n- CLAUDE.md line 63 or 75 has drifted from this task's expected text → grep for the relevant section header and update the closest match.\n\nKnown-bad to guard against:\n- A doc update that removes the `--from-json` examples entirely. Operators still use that form; the shim is permanent; the examples stay (with deprecation annotations).\n- A doc update that fails the `gen-docs --check` CI gate. Run `cargo run --bin gen-docs -- --check` locally before committing.",
      "edgeCases": [
        "gen-docs diff in tasks.md: commit alongside hand-edited docs",
        "README example matches new canonical form: no annotation needed",
        "CLAUDE.md line numbers have drifted: grep section headers, not line numbers"
      ],
      "estimatedEffort": "low",
      "failureModes": [
        {
          "cause": "gen-docs --check fails post-commit",
          "expectedBehavior": "Re-run `cargo run --bin gen-docs` (without --check); commit the resulting diff; verify --check passes"
        }
      ],
      "id": "FEAT-006",
      "modifiesBehavior": false,
      "notes": "Reuse anchors:\n- `cargo run --bin gen-docs` — regenerates `.claude/commands/tasks.md`. CI runs `--check` variant.\n- README.md current sections at lines 64, 328, 465 — the prompt's migration matrix calls these out.\n- CLAUDE.md (project) lines 63 (picker), 75 (worktree+PRD).\n\nLearning [951, 961, 996]: writes to `.claude/commands/` are denied in Claude Code don't-ask permission mode. This task runs in INTERACTIVE mode (the loop default for this PR), so it can write. If running in headless CI, the gen-docs binary writes the file natively (not Claude) so this is fine.\n\nGotcha: gen-docs writes via the `splice_block` and `write_atomic` helpers that FEAT-001 moved to `src/util/marker_splice.rs`. Make sure FEAT-001 is complete before running gen-docs here (the build will fail otherwise).",
      "passes": false,
      "priority": 6,
      "qualityDimensions": [
        "gen-docs --check passes (CI gate)",
        "Deprecated examples preserved + annotated (operator continuity)",
        "New canonical forms documented with at least one example each",
        "Deprecation policy note in CLAUDE.md prevents future engineers from removing the shim"
      ],
      "taskType": "implementation",
      "title": "Regenerate docs + update operator-facing docs",
      "touchesFiles": [
        "README.md",
        "CLAUDE.md",
        ".claude/commands/tasks.md"
      ]
    },
    {
      "acceptanceCriteria": [
        "No code duplication across enhance/, init/init_project, and main.rs dispatch synthesis (DRY)",
        "Functions under 30 lines (flag and refactor anything longer)",
        "Clear separation: marker_splice util has zero project-specific knowledge; enhance/ knows about TASK_MGR markers only; init/ knows about .task-mgr/ scaffolding only",
        "src/main.rs dispatch arms are readable — if any arm exceeds 25 lines, extract to a helper function in main.rs or a new module",
        "Pattern consistency: EnhanceResult mirrors SetupFix struct shape (name/description/applied/error) per Learning [1075]",
        "Any issues found spawn REFACTOR-FIX-xxx tasks via `task-mgr add --stdin --depended-on-by REVIEW-001`",
        "If no issues found, invoke /simplify on the largest touchpoint (likely src/main.rs dispatch) for a final read-through, then emit `<task-status>REFACTOR-001:done</task-status>`"
      ],
      "dependsOn": [
        "FEAT-001",
        "FEAT-002",
        "FEAT-003",
        "FEAT-004",
        "FEAT-005",
        "FEAT-006"
      ],
      "description": "Review all implementation across FEAT-001..006 for DRY violations, complexity, coupling, and pattern consistency.\n\nDO: Check for duplicated logic (e.g. marker handling in multiple places, dispatch synthesis in main.rs that should be a helper), functions >30 lines, tight coupling between modules. Look specifically at:\n- src/main.rs dispatch arms — did the Loop/Batch/Init shim logic grow into a function that should be extracted?\n- src/commands/enhance/mod.rs — is target-resolution logic reusable, or hardcoded inside handle_agents?\n- src/commands/init/mod.rs — is init_project sharing helpers with init() cleanly, or duplicating directory-creation code?\n- src/cli/tests.rs — are the ~40 pattern-update commits leaving behind dead test helpers?\n\nDO NOT: Skip this gate — it catches issues before REVIEW-001's full-suite run.",
      "estimatedEffort": "high",
      "id": "REFACTOR-001",
      "model": "claude-opus-4-7",
      "notes": "If issues found: `echo '{...}' | task-mgr add --stdin --depended-on-by REVIEW-001` for each (priority 50-97) — atomic DB+JSON sync, no manual JSON edit. If no issues, invoke /simplify on the ugliest touchpoint (typically the dispatch logic in src/main.rs after FEAT-004 and FEAT-005 land), then emit `<task-status>REFACTOR-001:done</task-status>` with a one-line progress note.",
      "passes": false,
      "priority": 98,
      "qualityDimensions": [
        "DRY across enhance/, init/, and main.rs dispatch",
        "Functions under 30 lines (or have a clear reason to be longer)",
        "Module boundaries clean — marker_splice/ has no project-specific knowledge",
        "EnhanceResult matches SetupFix struct shape (consistency with existing setup pattern)"
      ],
      "taskType": "review",
      "timeoutSecs": 1800,
      "title": "Review for refactoring opportunities",
      "touchesFiles": [
        "src/util/marker_splice.rs",
        "src/commands/enhance/mod.rs",
        "src/commands/enhance/templates.rs",
        "src/commands/init/mod.rs",
        "src/cli/commands.rs",
        "src/main.rs",
        "src/cli/tests.rs"
      ]
    },
    {
      "acceptanceCriteria": [
        "Positive: All new code reachable from production entry points — `task-mgr --help` shows enhance, `task-mgr loop --help` shows init+run, `task-mgr batch --help` shows init+run, `task-mgr init --help` shows --enhance",
        "Positive: All errors propagated with context (map_err with TaskMgrError variants, not unwrap)",
        "Positive: FULL `cargo test` (no scope), `cargo clippy -- -D warnings`, `cargo fmt --check`, and `cargo run --bin gen-docs -- --check` all clean",
        "Positive: shim smoke test passes — `task-mgr init --from-json <fixture>.json` in fresh tmpdir produces stderr deprecation + correct .task-mgr/ + correct PRD rows (verify via `task-mgr list --format json`)",
        "Positive: GOLDEN-FILE equivalence test (from FEAT-005) passes — shim and canonical path produce identical DB state",
        "Negative: No unwrap() in production code paths in any new file (grep `unwrap()` in src/commands/enhance/, src/commands/init/init_project body, src/util/marker_splice.rs — zero hits)",
        "Negative: No dead code warnings for any new function",
        "Negative: src/cli/tests.rs has no stale `Commands::Loop { prd_file, .. }` matches that bypass the new cmd field — every Loop/Batch match handles the new shape (Learning [1069])",
        "Documentation: CLAUDE.md (project) has 'Deprecation policy' subsection (verified by FEAT-006)",
        "Documentation: README has 'task-mgr enhance' subsection with at least one example per subcommand (verified by FEAT-006)",
        "Learning hygiene: `task-mgr learn --invalidate 1424` executed (or the equivalent CLI invocation); recall no longer surfaces '[1424] task-mgr init requires --from-json'",
        "Task update: remaining tasks reviewed; if any FEAT's API drifted during implementation (e.g., EnhanceResult shape changed), task descriptions/criteria updated via `task-mgr init --from-json tasks/init-split-and-enhance.json --append --update-existing`",
        "Pre-existing test failures fixed (or spawned as FIX-xxx with verifyCommand if >~12 unrelated)",
        "If issues found: FIX-xxx tasks spawned via `task-mgr add --stdin --depended-on-by REVIEW-001`"
      ],
      "dependsOn": [
        "FEAT-001",
        "FEAT-002",
        "FEAT-003",
        "FEAT-004",
        "FEAT-005",
        "FEAT-006",
        "REFACTOR-001",
        "REFACTOR-FIX-001"
      ],
      "description": "Review all implementation for quality, security, integration wiring, and documentation. RUN THE FULL QUALITY GATE (unscoped test suite).\n\nDO:\n- Check every new function in enhance/ and init/init_project for error handling (no unwrap() in production paths; all DB/file errors map_err'd with context).\n- Verify integration wiring: `task-mgr enhance` reachable from `task-mgr --help`; `task-mgr loop init` and `task-mgr batch init` reachable from their parents' help; `task-mgr init --enhance` documented in --help.\n- Verify Learning [1069] regression: grep `src/cli/tests.rs` for `Commands::Loop {` and `Commands::Batch {` — every match reflects the new variant shape (cmd + optional flat fields), no stale references to the old required-positional shape.\n- Run the FULL `cargo test` (no scope), `cargo clippy -- -D warnings`, `cargo fmt --check`, and `cargo run --bin gen-docs -- --check`. Fix every failure including pre-existing ones (escape hatch: >~12 unrelated failures → spawn a FIX-xxx with the failures listed and BLOCK).\n- Invalidate Learning [1424] via `task-mgr learn --invalidate 1424` since 'task-mgr init requires --from-json' is no longer true after FEAT-005.\n- Update any remaining task descriptions or acceptance criteria if implementation changed APIs or assumptions during the run (via `task-mgr init --from-json tasks/init-split-and-enhance.json --append --update-existing` — which itself goes through the new shim path and should keep working).\n- Verify the shim path with a manual smoke: in a fresh tmpdir, run `task-mgr init --from-json <fixture>.json` and confirm stderr deprecation appears + .task-mgr/ scaffolds + PRD imports.\n\nDO NOT:\n- Skip the integration wiring check — unconnected code is the #1 failure mode.\n- Scope the test suite — this is THE place the full suite runs.\n- Auto-pass without running the actual gen-docs --check gate.\n- Forget to invalidate Learning [1424] — leaving it in the recall index will mislead future agents on this codebase.",
      "estimatedEffort": "medium",
      "id": "REVIEW-001",
      "model": "claude-opus-4-7",
      "notes": "Spawn fixes via `echo '{...}' | task-mgr add --stdin --depended-on-by REVIEW-001` — DB + PRD JSON synced atomically. If no issues: emit `<task-status>REVIEW-001:done</task-status>` with 'Clean review' note.\n\nLearning invalidation: `task-mgr learn --invalidate 1424` after this PR merges. The learning text was 'task-mgr init requires --from-json' which is no longer true. Verify via `task-mgr recall --query 'init requires from-json'` returning zero or only superseded hits.\n\nManual smoke checklist:\n1. mkdir /tmp/tm-test && cd /tmp/tm-test\n2. target/debug/task-mgr init                                          # scaffolds + enhance hint\n3. target/debug/task-mgr init                                          # idempotent\n4. target/debug/task-mgr enhance show --profile workflow               # prints workflow block\n5. target/debug/task-mgr enhance agents --create                       # creates CLAUDE.md + AGENTS.md\n6. target/debug/task-mgr enhance agents --create                       # idempotent (no diff)\n7. target/debug/task-mgr enhance strip                                 # removes blocks\n8. echo '{\"tasks\":[{\"id\":\"X-001\",\"title\":\"t\",\"difficulty\":\"low\"}]}' > p.json\n9. target/debug/task-mgr init --from-json p.json                       # stderr DEPRECATED; row inserted\n10. target/debug/task-mgr loop init p.json --append --update-existing  # no diff\n11. target/debug/task-mgr loop p.json --yes --hours 0                  # flat-form deprecation + run\n\nLearning [1093]: file-writing fix functions are idempotent — confirm in enhance tests.\nLearning [1075]: SetupFix struct shape — confirm EnhanceResult matches.\nLearning [1505]: FK delete order — relevant to `loop init --force` and `batch init --force` paths; verify they still work post-refactor.",
      "passes": false,
      "priority": 99,
      "qualityDimensions": [
        "No unwrap in production paths in new code",
        "All new commands reachable from --help (no orphaned features)",
        "Full suite green including pre-existing failures (or documented FIX-xxx for >12-failure escape)",
        "Documentation matches behavior — gen-docs --check passes, README has new sections",
        "Learning [1424] invalidated — recall no longer misleads agents about this codebase",
        "Manual smoke passes for all 11 steps"
      ],
      "taskType": "review",
      "timeoutSecs": 1800,
      "title": "Code review + final verification",
      "touchesFiles": [
        "src/util/marker_splice.rs",
        "src/commands/enhance/mod.rs",
        "src/commands/enhance/templates.rs",
        "src/commands/enhance/tests.rs",
        "src/commands/init/mod.rs",
        "src/commands/init/tests.rs",
        "src/cli/commands.rs",
        "src/main.rs",
        "src/cli/tests.rs",
        "README.md",
        "CLAUDE.md",
        ".claude/commands/tasks.md"
      ]
    },
    {
      "acceptanceCriteria": [
        "PrefixMode::from_cli_flags helper exists in src/commands/init/mod.rs",
        "src/main.rs has zero remaining direct PrefixMode::Disabled/Explicit/Auto if-else chains; one helper call per dispatch site",
        "All existing tests pass with no behavior change",
        "No new clippy warnings"
      ],
      "description": "PrefixMode resolution from (no_prefix, prefix) CLI flags is duplicated 3x in src/main.rs: lines 185-191 (dispatch_init mode 3), lines 1005-1011 (LoopCommand::Init), lines 1144-1150 (BatchCommand::Init). All three blocks are byte-identical. DRY violation introduced by FEAT-004/005.",
      "estimatedEffort": "low",
      "id": "b848fe20-REFACTOR-FIX-001",
      "passes": false,
      "priority": 88,
      "title": "Extract PrefixMode CLI resolution helper",
      "touchesFiles": [
        "src/commands/init/mod.rs",
        "src/main.rs"
      ]
    }
  ],
  "version": "1.0"
}
